import {NextRequest,NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";import {importActionSchema} from "@/lib/customer/onboarding-import-schema";import {hasTrustedMutationOrigin} from "@/lib/security/same-origin";import {isApplicationJson,parseJsonWithLimit} from "@/lib/security/request-body";import {mapOnboardingError,ONBOARDING_HEADERS} from "@/lib/customer/onboarding-import-helper";
const actions={validate:"validate_import_v1","dry-run":"dry_run_import_v1",submit:"submit_import_v1","approve-commit":"approve_import_commit_v1",activate:"activate_import_v1"} as const;
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string;action:string}>}){if(!hasTrustedMutationOrigin(req))return NextResponse.json({error:{code:"BAD_ORIGIN"}},{status:403,headers:ONBOARDING_HEADERS});if(!isApplicationJson(req.headers.get("content-type")))return NextResponse.json({error:{code:"UNSUPPORTED_MEDIA_TYPE"}},{status:415,headers:ONBOARDING_HEADERS});const {id,action}=await params;const rpc=actions[action as keyof typeof actions];const {data:body,errorResponse}=await parseJsonWithLimit<unknown>(req,8*1024);if(errorResponse)return errorResponse;const p=importActionSchema.safeParse(body);if(!rpc||!p.success)return NextResponse.json({error:{code:"INVALID_REQUEST"}},{status:400,headers:ONBOARDING_HEADERS});const s=await createClient();const {data,error}=await s.schema("customer_api").rpc(rpc as never,{p_context_id:p.data.context_id,p_run_id:id} as never);if(error){const e=mapOnboardingError(error);return NextResponse.json({error:{code:e.code}},{status:e.status,headers:ONBOARDING_HEADERS});}return NextResponse.json(data,{headers:ONBOARDING_HEADERS});}
