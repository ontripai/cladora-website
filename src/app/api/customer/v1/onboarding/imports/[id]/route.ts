import { NextRequest,NextResponse } from "next/server";import {createClient} from "@/lib/supabase/server";import {importContextSchema} from "@/lib/customer/onboarding-import-schema";import {hasTrustedMutationOrigin} from "@/lib/security/same-origin";import {mapOnboardingError,ONBOARDING_HEADERS} from "@/lib/customer/onboarding-import-helper";
const idOk=(id:string)=>/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(id);
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const p=importContextSchema.safeParse({context_id:req.nextUrl.searchParams.get("context_id")});if(!idOk(id)||!p.success)return NextResponse.json({error:{code:"INVALID_REQUEST"}},{status:400,headers:ONBOARDING_HEADERS});const s=await createClient();const {data,error}=await s.schema("customer_api").rpc("get_import_preview_v1" as never,{p_context_id:p.data.context_id,p_run_id:id} as never);if(error){const e=mapOnboardingError(error);return NextResponse.json({error:{code:e.code}},{status:e.status,headers:ONBOARDING_HEADERS});}return NextResponse.json(data,{headers:ONBOARDING_HEADERS});}
export async function DELETE(req:NextRequest,{params}:{params:Promise<{id:string}>}){if(!hasTrustedMutationOrigin(req))return NextResponse.json({error:{code:"BAD_ORIGIN"}},{status:403,headers:ONBOARDING_HEADERS});const {id}=await params;const p=importContextSchema.safeParse({context_id:req.nextUrl.searchParams.get("context_id")});if(!idOk(id)||!p.success)return NextResponse.json({error:{code:"INVALID_REQUEST"}},{status:400,headers:ONBOARDING_HEADERS});const s=await createClient();const {data,error}=await s.schema("customer_api").rpc("cancel_import_v1" as never,{p_context_id:p.data.context_id,p_run_id:id} as never);if(error){const e=mapOnboardingError(error);return NextResponse.json({error:{code:e.code}},{status:e.status,headers:ONBOARDING_HEADERS});}return NextResponse.json(data,{headers:ONBOARDING_HEADERS});}
